#pragma once

#include <string>
#include <string_view>
#include <cstddef>

namespace mntstr {
    using string = std::string;
    using sv = std::string_view;
    using s_t = std::size_t;
    using uc = unsigned char;
}